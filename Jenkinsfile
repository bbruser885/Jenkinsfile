pipeline {

    agent any

    stages {
        stage("build") {
            echo 'building the app'
        }
        stage("test") {
            echo 'testing the app'
        }
        stage("deploy") {
            echo 'deploying this sob'
        }
    }
}